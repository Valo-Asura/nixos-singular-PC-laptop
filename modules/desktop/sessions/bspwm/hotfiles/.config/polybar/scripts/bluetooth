#!/usr/bin/env bash
set -euo pipefail

if ! bluetoothctl show 2>/dev/null | grep -q "Powered: yes"; then
  echo ""
else
  if ! echo info | bluetoothctl 2>/dev/null | grep -q 'Device'; then
    echo "󰂯"
  else
    echo "󰂱"
  fi
fi
