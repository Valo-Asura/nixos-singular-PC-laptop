# vibewallREzero

Disabled future source package only.

- Kept under `packages/vibewallREzero/`
- Not imported
- Not enabled
- Shared active wallpaper backend is `skwd-wall`

This directory stays in the repo as a parked future source tree. Screenshot
proofs and active workflow docs were removed so the repo surface matches the
current shared-shell structure.
# disabled for now; skwd-wall is active.
